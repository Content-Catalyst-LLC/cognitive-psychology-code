# Notebooks

The notebook in this directory is a lightweight reproducible workflow scaffold. Generate the dataset first:

```bash
python3 ../python/problem_solving_model.py --simulate --output ../data/problem_solving_trials.csv --outputs ../outputs
```

Then open:

```bash
jupyter notebook problem_solving_workflow.ipynb
```
