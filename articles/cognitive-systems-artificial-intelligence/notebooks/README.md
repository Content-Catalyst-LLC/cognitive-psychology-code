# Notebooks

The notebook in this directory is a lightweight reproducible workflow scaffold. Generate the dataset first:

```bash
python3 ../python/cognitive_systems_ai_model.py --simulate --output ../data/cognitive_systems_trials.csv --outputs ../outputs
```

Then open:

```bash
jupyter notebook cognitive_systems_ai_workflow.ipynb
```
