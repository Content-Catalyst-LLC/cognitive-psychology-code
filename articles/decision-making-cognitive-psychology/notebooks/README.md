# Notebooks

Generate the dataset first:

```bash
python3 ../python/decision_model.py --simulate --output ../data/decision_trials.csv --outputs ../outputs
```

Then open:

```bash
jupyter notebook decision_workflow.ipynb
```
