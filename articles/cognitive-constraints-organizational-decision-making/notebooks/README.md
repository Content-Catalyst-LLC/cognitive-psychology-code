# Notebooks

The notebook in this directory is a lightweight reproducible workflow scaffold. Generate the dataset first:

```bash
python3 ../python/cognitive_constraints_model.py --simulate --output ../data/organizational_decision_trials.csv --outputs ../outputs
```

Then open:

```bash
jupyter notebook cognitive_constraints_workflow.ipynb
```
