# Notebooks

Generate the dataset first:

```bash
python3 ../python/cognitive_bias_model.py --simulate --output ../data/cognitive_bias_trials.csv --outputs ../outputs
```

Then open:

```bash
jupyter notebook cognitive_bias_workflow.ipynb
```
