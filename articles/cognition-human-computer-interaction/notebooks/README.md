# Notebooks

The notebook in this directory is a lightweight reproducible workflow scaffold. Generate the dataset first:

```bash
python3 ../python/hci_cognition_model.py --simulate --output ../data/hci_trials.csv --outputs ../outputs
```

Then open:

```bash
jupyter notebook hci_cognition_workflow.ipynb
```
