# Notebooks

The notebook in this directory is a lightweight reproducible workflow scaffold. Generate the dataset first:

```bash
python3 ../python/analogical_reasoning_model.py --simulate --output ../data/analogical_reasoning_trials.csv --outputs ../outputs
```

Then open:

```bash
jupyter notebook analogical_reasoning_workflow.ipynb
```
