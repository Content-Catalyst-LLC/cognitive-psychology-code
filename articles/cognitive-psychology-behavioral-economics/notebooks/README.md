# Notebooks

The notebook in this directory is a lightweight reproducible workflow scaffold. Generate the dataset first:

```bash
python3 ../python/behavioral_economics_model.py --simulate --output ../data/behavioral_economics_trials.csv --outputs ../outputs
```

Then open:

```bash
jupyter notebook behavioral_economics_workflow.ipynb
```
