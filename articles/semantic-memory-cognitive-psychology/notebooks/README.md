# Notebooks

The notebook in this directory is a lightweight reproducible workflow scaffold. Generate the dataset first:

```bash
python3 ../python/semantic_memory_model.py --simulate --output ../data/semantic_memory_trials.csv --outputs ../outputs
```

Then open:

```bash
jupyter notebook semantic_memory_workflow.ipynb
```
