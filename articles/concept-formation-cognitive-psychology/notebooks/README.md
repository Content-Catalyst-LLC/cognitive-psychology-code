# Notebooks

The notebook in this directory is a lightweight reproducible workflow scaffold. Generate the dataset first:

```bash
python3 ../python/concept_formation_model.py --simulate --output ../data/concept_formation_trials.csv --outputs ../outputs
```

Then open:

```bash
jupyter notebook concept_formation_workflow.ipynb
```
