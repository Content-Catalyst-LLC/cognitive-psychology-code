# Notebooks

The notebook in this directory is a lightweight reproducible workflow scaffold. Generate the dataset first:

```bash
python3 ../python/language_processing_model.py --simulate --output ../data/language_processing_trials.csv --outputs ../outputs
```

Then open:

```bash
jupyter notebook language_processing_workflow.ipynb
```
