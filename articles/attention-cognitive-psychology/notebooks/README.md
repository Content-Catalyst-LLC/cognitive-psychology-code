# Notebooks

Generate the dataset first:

```bash
python3 ../python/attention_model.py --simulate --output ../data/attention_trials.csv --outputs ../outputs
```

Then open:

```bash
jupyter notebook attention_workflow.ipynb
```
