# Notebook workflow

Generate the dataset:

```bash
python3 ../python/skill_acquisition_model.py --simulate --output ../data/skill_acquisition_trials.csv --outputs ../outputs
```

Then analyze learning curves, novice-expert differences, feedback, transfer, retention, and automaticity in a notebook of your choice.
